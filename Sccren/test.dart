import 'package:flutter/material.dart';

class Textxx extends StatelessWidget {
  static const routeNamed = '/test';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
